#include <bits/stdc++.h>
using namespace std;

string str;
int cnt;
int main() {
    while (true) {
        getline(cin, str);
        if (str == "0")
            break;
        cout << "Case " << ++cnt << ": " << "Sorting... done!" << '\n';
    }
}